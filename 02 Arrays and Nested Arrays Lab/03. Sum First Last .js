function solve(inputArray) {
    let firstEl = Number(inputArray[0]);
    let lastEl = Number(inputArray[inputArray.length -1]);

    let sum = firstEl + lastEl;

    console.log(sum);
}

solve(['20', '30', '40'] );